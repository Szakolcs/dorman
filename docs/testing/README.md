# Testing documentation

**Status:** placeholder — content not yet checked in under `docs/testing/`.

This folder is reserved for human-readable test catalogues and testing-process docs referenced from the repository [README](../../README.md#testing-strategy), for example:

- System test case IDs and scenarios (`testing/system` branch)
- Mapping from cases to requirements (`FR-*`) and use cases (`UC-*`)

Executable tests live in the Go tree (`internal/integration/`, `internal/systemtest/`). Run commands and environment variables are documented in the root README.
