# Developer documentation

**Status:** placeholder — content not yet checked in under `docs/dev/`.

This folder is reserved for engineer-facing material referenced from the repository [README](../../README.md#documentation-pointers), for example:

- Local setup (Docker, Air, environment variables)
- Architecture and package layout
- Admin HTTP surfaces and development stubs
- Migrations, seeding (`cmd/seed`), and test targets (`make integration-test`, `make system-test`)

Until those guides exist here, use the root README **Testing Strategy**, **Demo database catalog**, and **Documentation pointers** sections, plus module specs under [../specifications/](../specifications/).

Planned companion: `branch-workflow.md` (branch lifecycle from `docs/main` through `development/main` and testing gates).
