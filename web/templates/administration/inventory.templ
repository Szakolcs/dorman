package adminviews

import "dorm-man/internal/models"

templ InventoryPage(items []models.InventoryItem) {
	@Layout("Inventory") {
		@Navigation()
	}
}

templ InventoryDetailPage(item models.InventoryItem) {
	@Layout("Inventory Item") {
		@Navigation()
	}
}
