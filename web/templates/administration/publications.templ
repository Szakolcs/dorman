package adminviews

import "dorm-man/internal/models/forum"

templ PublicationsPage(publications []forum.Publication) {
	@Layout("Publications") {
		@Navigation()
	}
}

templ NewsDetailPage(news forum.Publication) {
	@Layout("News") {
		@Navigation()
	}
}

templ ActivityDetailPage(activity forum.Activity) {
	@Layout("Activity") {
		@Navigation()
	}
}

templ EventDetailPage(event forum.Event) {
	@Layout("Event") {
		@Navigation()
	}
}
