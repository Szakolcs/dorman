package adminviews

import "dorm-man/internal/models"

templ TenantsPage(tenants []models.Tenant) {
	@Layout("Tenants") {
		@Navigation()
	}
}

templ TenantDetailPage(tenant models.Tenant) {
	@Layout("Tenant") {
		@Navigation()
	}
}
