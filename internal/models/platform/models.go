package models

// Platform models package provides shared primitives (BaseModel, PublicationState).
//
// Identity and RBAC tables (User, Role, UserRole) are defined and migrated via
// internal/models/administration — see administration/user.go and administration/models.go.
